Similar data stored in continuous memory
 
- Referential Arrays - Stores references rather than actual data
- Dynamic Arrays - Dynamic memory allocation  

**Lists - are both referential and dynamic**
Since lists are referential they don't have to store the same kind of data

- import [[ctypes]]

