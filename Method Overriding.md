- In python we can have a child class whose method can override the parent class's method.
![[Pasted image 20260322012037.png|360]]

In this case it area() of the child class is overriding the area of the shape class, this is useful in extending the functionality of a method rather than completely overriding it.